# XRT runtime (place next to host executable on the board, or set XRT_INI_PATH).
# Used when EN_TRACE=1 in config.json — see Makefile PKG_FLAGS.
#
# After run: copy generated *.run_summary / profile_summary / timeline_trace to host;
# open with: vitis_analyzer <file>.run_summary
#
# Full key list: AMD/XRT docs — "Configuration File xrt.ini"

[Debug]
profile=true
timeline_trace=true
data_transfer_trace=fine
